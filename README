A simple command-line utility to automate/simplify deletion of data sets or maps from a GeoIQ library.

Written in C#, tested against the .Net framework 4.0.

Makes use of GeoIQ.Net library, a wrapper around the GeoIQ HTTP interface and found in the 'libs' folder. 
The source code for this library will be coming soon so the binaries have been provided for now.